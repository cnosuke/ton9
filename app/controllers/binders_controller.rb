class BindersController < ApplicationController
  #バインダの作成
  # POST /users/:user_id/documents/:document_id
  def create
  end
  
  #既存のドキュメントをバインダに追加するアクション
  # POST /users/:user_id/documents/:document_id/:bid
  def add_documents
  end
end
