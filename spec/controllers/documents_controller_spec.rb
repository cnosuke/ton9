require 'spec_helper'

describe DocumentsController do
  describe :create do
    context "成功した時" do
      it "Documentが作成されている"
      it "statusとしてsuccessが返される"
      it "encodingとして??が返される"
      pending "まだあるかも"
    end
    context "失敗した時" do
      it "Documentが作成されない"
      it "statusとしてfailureが返される"
      it "encodingとして??が返される"
      pending "まだあるかも"
    end
  end
end
